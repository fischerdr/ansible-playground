{
  "backup_locations": [
    {
      "name": "nfs-backup-location",
      "location_type": "NFS",
      "path": "/mnt/backups",
      "encryption_key": "my-encryption-key",
      "nfs_config": {
        "server": "nfs.example.com",
        "export_path": "/exports/backups"
      }
    },
    {
      "name": "s3-backup-location",
      "location_type": "S3",
      "path": "px-backups",
      "encryption_key": "my-s3-encryption-key",
      "cloud_cred_name": "s3-credentials",
      "validate_cloud_credential": true,
      "object_lock_enabled": true,
      "s3_config": {
        "region": "us-west-2",
        "endpoint": "s3.amazonaws.com",
        "disable_ssl": false
      }
    },
    {
      "name": "azure-backup-location",
      "location_type": "Azure",
      "path": "px-azure-backups",
      "cloud_cred_name": "azure-credentials",
      "azure_config": {
        "storage_account": "pxbackupstorage",
        "resource_group": "px-backup-rg"
      }
    },
    {
      "name": "google-backup-location",
      "location_type": "Google",
      "path": "px-gcp-backups",
      "cloud_cred_name": "gcp-credentials",
      "google_config": {
        "project_id": "px-backup-project"
      }
    },    {
      "name": "{{ cluster_user}}-{{ cluster_env }}-{{ region }}-{{ zone }}",
      "location_type": "S3",
      "path": "{{ cluster_user }}-{{ cluster_env }}-{{ region }}-{{ zone }}",
      "encryption_key": "my-s3-encryption-key",
      "cloud_cred_name": "s3-credentials-{{ cluster_env }}",
      "validate_cloud_credential": true,
      "object_lock_enabled": true,
      "s3_config": {
        "region": "{{ region | default('us-west-2') }}",
        "endpoint": "{{ if cluster_env == 'dev' or cluster_env == 'test' }}s3-devtest.amazonaws.com{{ elif region is defined and region.startswith('eusw') }}myurl{{ elif region is defined and region.startswith('euse') }}myeasturl{{ else }}s3.amazonaws.com{{ endif }}",
        "disable_ssl": false
      }
    }
  ]
}
